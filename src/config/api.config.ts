export const API_CONFIG = {
  // Con el proxy configurado, podemos usar una ruta relativa
  BASE_URL: "/api",
  HEADERS: {
    "Content-Type": "application/json",
    Accept: "application/json",
  },
};
